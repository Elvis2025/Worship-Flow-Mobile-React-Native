export const userTableStatements = [
  `CREATE TABLE IF NOT EXISTS users (id TEXT PRIMARY KEY NOT NULL, tenantId TEXT NOT NULL, payloadJson TEXT NOT NULL, updatedAt TEXT NOT NULL, syncedAt TEXT, isDirty INTEGER NOT NULL DEFAULT 0, isDeleted INTEGER NOT NULL DEFAULT 0);`,
  `CREATE TABLE IF NOT EXISTS roles (id TEXT PRIMARY KEY NOT NULL, tenantId TEXT, payloadJson TEXT NOT NULL, updatedAt TEXT NOT NULL, syncedAt TEXT, isDirty INTEGER NOT NULL DEFAULT 0, isDeleted INTEGER NOT NULL DEFAULT 0);`,
  `CREATE TABLE IF NOT EXISTS permissions (id TEXT PRIMARY KEY NOT NULL, tenantId TEXT, payloadJson TEXT NOT NULL, updatedAt TEXT NOT NULL, syncedAt TEXT, isDirty INTEGER NOT NULL DEFAULT 0, isDeleted INTEGER NOT NULL DEFAULT 0);`,
  `CREATE TABLE IF NOT EXISTS user_roles (id TEXT PRIMARY KEY NOT NULL, tenantId TEXT NOT NULL, userId TEXT NOT NULL, roleId TEXT NOT NULL, payloadJson TEXT, updatedAt TEXT NOT NULL, syncedAt TEXT, isDirty INTEGER NOT NULL DEFAULT 0, isDeleted INTEGER NOT NULL DEFAULT 0);`,
  `CREATE TABLE IF NOT EXISTS user_permissions (id TEXT PRIMARY KEY NOT NULL, tenantId TEXT NOT NULL, userId TEXT NOT NULL, permissionCode TEXT NOT NULL, payloadJson TEXT, updatedAt TEXT NOT NULL, syncedAt TEXT, isDirty INTEGER NOT NULL DEFAULT 0, isDeleted INTEGER NOT NULL DEFAULT 0);`,
  `CREATE TABLE IF NOT EXISTS user_availability (id TEXT PRIMARY KEY NOT NULL, tenantId TEXT NOT NULL, userId TEXT NOT NULL, payloadJson TEXT NOT NULL, updatedAt TEXT NOT NULL, syncedAt TEXT, isDirty INTEGER NOT NULL DEFAULT 0, isDeleted INTEGER NOT NULL DEFAULT 0);`,
  `CREATE TABLE IF NOT EXISTS sync_queue (id TEXT PRIMARY KEY NOT NULL, operationType TEXT NOT NULL, entityName TEXT NOT NULL, entityId TEXT NOT NULL, payload TEXT NOT NULL, createdAt TEXT NOT NULL, retryCount INTEGER NOT NULL DEFAULT 0, status TEXT NOT NULL);`,
];
